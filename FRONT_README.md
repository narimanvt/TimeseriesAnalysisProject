  # Time Series Analysis Web app

  This is a code bundle for KNTU Time series analysis.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  